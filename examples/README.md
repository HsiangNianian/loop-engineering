# Examples

Examples are small executable demonstrations, not benchmarks.

Run the deterministic recovery example without credentials or network access:

```bash
uv run python examples/basic.py
```

The example intentionally fails its first verification and succeeds on its second action. Future
examples should remain focused on one loop invariant and link to an evaluation when making a
performance or reliability claim.
