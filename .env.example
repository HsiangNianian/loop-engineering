# Required for live OpenAI calls. Never commit a real key.
OPENAI_API_KEY=

# Optional; this is the repository default.
OPENAI_MODEL=gpt-5.6

# Optional custom OpenAI-compatible endpoint. Leave blank to use the SDK default.
# Example: OPENAI_BASEURL=https://gateway.example.com/v1
OPENAI_BASEURL=
